name: Finalize Trader OS v3.0.2 ready

on:
  push:
    branches: [v3.0.2-ready]

permissions:
  contents: write

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v3.0.2-ready
          fetch-depth: 0

      - name: Apply embedded v3.0.2 changes
        shell: bash
        run: |
          sed -n "/python3 - <<'PY'/,/^[[:space:]]*PY$/p" .github/workflows/build-v3.0.2-ready.yml | sed '1d;$d;s/^          //' > /tmp/apply_v302.py
          python3 /tmp/apply_v302.py
          python3 - <<'PY'
          from pathlib import Path
          for p in [Path('server/package.json'),Path('web/package.json')]:
              s=p.read_text()
              s=s.replace('"@trader-os/shared": "3.0.1"','"@trader-os/shared": "3.0.2"')
              p.write_text(s)
          PY

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Validate tests and build
        run: |
          npm install
          npm test
          npm run build

      - name: Commit full ready source
        shell: bash
        run: |
          rm -f .github/workflows/build-v3.0.2-ready.yml .github/workflows/finalize-v3.0.2-ready.yml .v302-trigger
          git config user.name "Trader OS Builder"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Trader OS v3.0.2 ready source"
          git push origin HEAD:v3.0.2-ready
